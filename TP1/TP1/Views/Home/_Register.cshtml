@model TP1.Models.RegisterModel

@using (Html.BeginForm("JsonRegister", "Account", new { ViewBag.ReturnUrl }, FormMethod.Post, new { Id = "registerForm" }))
{
    @Html.AntiForgeryToken()

    <fieldset>
        <legend>Registration Form</legend>
        <ol>
            <li>
                @Html.KnockoutHelperFor(m => m.UserName)  
                @Html.TextBoxFor(m => m.UserName, new { id = "registerName" })
            </li>
            <li>
                @Html.KnockoutHelperFor(m => m.Password) 
                @Html.PasswordFor(m => m.Password)
            </li>
            <li>
                @Html.KnockoutHelperFor(m => m.ConfirmPassword)
                @Html.PasswordFor(m => m.ConfirmPassword)
            </li>
            <li>
                <input type="submit" value="Sign up" />
            </li>
        </ol>
        @Html.ValidationSummary()
    </fieldset>
}